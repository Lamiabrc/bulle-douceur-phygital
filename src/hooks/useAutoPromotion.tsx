import { useEffect } from 'react';
import { useAuth } from './useAuth';

/**
 * Hook désactivé - la fonction promote_first_admin n'est pas disponible
 */
export function useAutoPromotion() {
  // Désactivé temporairement car la RPC n'existe pas
  return;
}
